{
  "name": "shasta-land-map",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive Siskiyou County public auction and private land map",
  "scripts": {
    "dev": "vite",
    "start": "vite preview",
    "refresh": "node scripts/refresh-data.js",
    "build": "vite build",
    "test": "node scripts/test.js",
    "release": "npm run refresh && git add data && git commit -m \"refresh data $(date)\" && npm run test && npm run deploy",
    "deploy": "npm run build && gh-pages --dotfiles -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "pdfjs-dist": "^5.4.149",
    "vite": "^8.2.1",
    "vite-plugin-static-copy": "^4.1.1"
  },
  "dependencies": {
    "maplibre-gl": "^6.3.0",
    "pmtiles": "^4.5.0"
  }
}
